source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!

target 'VersionsTracker_Example', :exclusive => true do
  pod 'VersionsTracker', :path => '../'
end

target 'VersionsTracker_Tests', :exclusive => true do
  pod 'VersionsTracker', :path => '../'

  pod 'Quick', '~> 0.8.0'
  pod 'Nimble', '3.0.0'
end
